[tool.poetry]
name = "pinemarten"
version = "0.0.1"
description = "Data mapping framework for CDISC standards."
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include="pinemarten"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
